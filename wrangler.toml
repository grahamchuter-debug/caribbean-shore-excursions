name = "caribbean-shore-excursions"
compatibility_date = "2024-11-01"

# Output directory for the static export (generated by `npm run build`)
pages_build_output_dir = "out"

# NOTE: wrangler.toml does NOT run the build. You must set a build command in
# Cloudflare Dashboard → Settings → Builds → Build command:
#   npm run build
